<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>For Siang 💖</title>

  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      overflow: hidden;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 30px;
    }

    .buttons {
      position: relative;
      width: 300px;
      height: 150px;
    }

    button {
      padding: 15px 30px;
      font-size: 1.2rem;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: all 0.3s ease;
      position: absolute;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
      left: 40px;
      top: 40px;
    }

    #noBtn {
      background: #444;
      color: white;
      right: 40px;
      top: 40px;
    }

    #areYouSure {
      position: absolute;
      font-size: 1rem;
      color: white;
      background: rgba(0,0,0,0.6);
      padding: 6px 14px;
      border-radius: 20px;
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .footer-text {
      position: absolute;
      bottom: 15px;
      font-size: 1rem;
    }

    .fade-out {
      animation: fadeOut 0.6s forwards;
    }

    @keyframes fadeOut {
      to {
        opacity: 0;
        transform: scale(0.95);
      }
    }
  </style>
</head>

<body>

<h1>Siang will you be my Valentine?</h1>

<div class="buttons">
  <button id="yesBtn">YES</button>
  <button id="noBtn">NO</button>
  <div id="areYouSure">Are you sure?</div>
</div>

<div class="footer-text">No is not acceptable :-P</div>

<script>
  const noBtn = document.getElementById("noBtn");
  const yesBtn = document.getElementById("yesBtn");
  const areYouSure = document.getElementById("areYouSure");

  let yesScale = 1;
  let noClickCount = 0;

  noBtn.addEventListener("click", () => {
    noClickCount++;

    if (noClickCount === 1) {
      areYouSure.style.left = noBtn.offsetLeft + "px";
      areYouSure.style.top = (noBtn.offsetTop - 40) + "px";
      areYouSure.style.opacity = "1";
    } else {
      yesScale += 0.2;
      yesBtn.style.transform = `scale(${yesScale})`;
    }
  });

  yesBtn.addEventListener("click", () => {
    document.body.classList.add("fade-out");
    setTimeout(() => {
      window.location.href = "celebration.html";
    }, 600);
  });
</script>

</body>
</html>
